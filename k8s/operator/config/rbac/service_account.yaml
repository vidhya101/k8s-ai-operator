apiVersion: v1
kind: ServiceAccount
metadata:
  name: appworkload-operator
  namespace: appworkload-operator-system
automountServiceAccountToken: true   # the operator IS a Kubernetes API client — this is the one workload in the whole repo where that's correct
